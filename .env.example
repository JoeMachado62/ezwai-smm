# Database Configuration
DB_USERNAME=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_NAME=ezwai_smm

# Flask Configuration
FLASK_SECRET_KEY=your_secret_key_here_change_this
FLASK_DEBUG=False

# Email Configuration (SendGrid)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USERNAME=apikey
EMAIL_PASSWORD=your_sendgrid_api_key

# Replicate API (for SeeDream-4 image generation)
REPLICATE_API_TOKEN=your_replicate_token

# Note: Per-user API keys are stored in the database and automatically
# generated as .env.user_{user_id} files when users log in
